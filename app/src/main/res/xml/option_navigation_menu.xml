<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/brightness_option"
        android:enabled="true"
        android:icon="@drawable/brightness"
        android:tint="#fff"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/contrast_option"
        android:enabled="true"
        android:icon="@drawable/contrast"
        android:tint="#fff"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/saturation_option"
        android:enabled="true"
        android:icon="@drawable/saturation"
        android:tint="#fff"
        app:showAsAction="ifRoom" />
</menu>